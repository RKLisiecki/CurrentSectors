

sStructureChart <- function(country, path) {
  # Funkcja z opcjonalnymi parametrami nazwy państwa (country) oraz path
  # zwraca wykres powierzchniowy udziałów poszczególnych sektorów w kapitalizacji łącznej spółek.
  # Funkcja wykorzystuje przeliczenie odpowiednich wartości do Euro, gdy to konieczne.
  # Domyślnie struktura prezentowana jest dla całego zbioru spółek
  # a uzupełnienie parametru nazwy państwa zawęża rezultat geograficznie.
  # Wykres zawiera informatywny tytuł. Domyślnie funkcja wykorzystuje dane dołączone
  # a przy ustaleniu wartości parametru path ich wersje zapisane na dysku użytkownika (F.DB.1)
  return("StructureChart")
}
