

sStructure <- function(country, path) {
  # Funkcja z obligatoryjnym parametrem nazwy państwa (country) oraz
  # opcjonalnym parametrem path zwraca w formacie tabeli aktualny
  # zbiór wartości łącznych kapitalizacji (Market cap total) w Euro
  # oraz udziałów w kapitalizacji łącznej w państwie (Total market cap share)
  # wszystkich spółek skategoryzowanych w tych sektorach i branżach.
  # Funkcja przelicza odpowiednie wartości do Euro, jeżeli to konieczne.
  # Domyślnie funkcja wykorzystuje dane dołączone a przy ustaleniu wartości
  # parametru path ich wersje zapisane na dysku użytkownika (F.DB.1)
  return("SECTOR STRUCTURE |")
}
