# Funkcja z opcjonalnymi parametrami nazwy państwa (country) oraz path
# zwraca w formie tabeli (struktury data.frame) wartości łączne kapitalizacji
# spółek we wszystkich sektorach (kolumny), dla poszczególnych miesięcy
# z notowaniami dostępnymi w bazie szeregów czasowych (wiersze).
# Wartości przeliczone są do waluty Euro, na podstawie notowań poszczególnych
# spółek w zbiorze danych oraz notowań FX. Domyślnie prezentowane są wyliczenia
# dla całego zbioru spółek a uzupełnienie parametru wybranego państwa zawęża reultat geograficznie.
# Domyślnie funkcja wykorzystuje dane dołączone
# a przy ustaleniu wartości parametru path ich wersje zapisane na dysku użytkowanika (F.DB.1)

sIndices <- function(country, path) {
  # Funkcja z opcjonalnymi parametrami nazwy państwa (country) oraz path
  # zwraca w formie tabeli (struktury data.frame) wartości łączne kapitalizacji
  # spółek we wszystkich sektorach (kolumny), dla poszczególnych miesięcy
  # z notowaniami dostępnymi w bazie szeregów czasowych (wiersze).
  # Wartości przeliczone są do waluty Euro, na podstawie notowań poszczególnych
  # spółek w zbiorze danych oraz notowań FX. Domyślnie prezentowane są wyliczenia
  # dla całego zbioru spółek a uzupełnienie parametru wybranego państwa zawęża reultat geograficznie.
  # Domyślnie funkcja wykorzystuje dane dołączone
  # a przy ustaleniu wartości parametru path ich wersje zapisane na dysku użytkowanika (F.DB.1)
  return("INDICES TABLE")
}

